{
  "name": "pathology-informatics-interactives",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite --configLoader runner",
    "assets:optimize": "node scripts/optimize-images.mjs",
    "build": "tsc --noEmit && vite build --configLoader runner && node --import tsx scripts/export-coverage.ts && node scripts/create-pages-fallback.mjs",
    "build:pages": "tsc --noEmit && vite build --base /pathology-informatics-interactives/ --configLoader runner && node --import tsx scripts/export-coverage.ts && node scripts/create-pages-fallback.mjs",
    "test:curriculum": "node --import tsx scripts/validate-curriculum.ts",
    "test:unit": "node --import tsx --test tests/*.test.ts tests/*.test.tsx",
    "test": "npm run test:curriculum && npm run test:unit && npm run build"
  },
  "dependencies": {
    "@mdx-js/react": "^3.1.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@mdx-js/rollup": "^3.1.1",
    "@types/node": "^22.18.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "remark-gfm": "^4.0.1",
    "sharp": "^0.35.3",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
